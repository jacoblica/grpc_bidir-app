Create a protobuf file for a bidirectional streaming pattern, including one request and response message with 5 fields of different data types, including 1 a enum for opcode,  and then generate client and server code in C++ ad project cmake file, make sure they're full functional and tested. Leave me the place on both side to parse the  request and response message,  extract the opcode, and dispatch based on opcode.